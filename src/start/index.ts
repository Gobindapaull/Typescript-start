function greet(name: string): string {
    return `Hello ${name}`;
}

console.log(greet("Typescript"));

// Primitive data types

// SNB

// string
const symbol: string = "SOL"

// number
const x: number = 12345;

// boolean
const y: boolean = true;
