// Boolean
let buy: boolean = false;

// Number
let price: number = 1500;

// String
let Name: string = "Ethereum";

// Array
let count: number[] = [1, 2, 3, 4, 5];

// any
let anything: any = 4;
anything = "anything text";
anything = true;

// Enum
enum Directions {
    UP,
    DOWN,
    LEFT,
    RIGHT
}
